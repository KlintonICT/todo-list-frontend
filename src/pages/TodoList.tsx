import { Flex, Heading } from '@chakra-ui/react';

const TodoList = () => {
  return (
    <Flex alignItems="center" justifyContent="center" mt={20}>
      <Heading as="h5">Todo App</Heading>
    </Flex>
  );
};

export default TodoList;
